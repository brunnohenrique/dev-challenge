<%= form_with(model: borrower) do |form| %>
  <% if borrower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(borrower.errors.count, "error") %> prohibited this borrower from being saved:</h2>

      <ul>
        <% borrower.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :legal_name %>
    <%= form.text_field :legal_name %>
  </div>

  <div class="field">
    <%= form.label :cnpj %>
    <%= form.text_field :cnpj %>
  </div>

  <div class="actions">
    <i class="material-icons right"><%= form.submit :Submit, class: 'btn waves-effect waves-light' %></i>
    <i class="material-icons right"><%= link_to 'Back', borrowers_path, class: 'btn waves-effect deep-orange-2' %></i>
  </div>
<% end %>
